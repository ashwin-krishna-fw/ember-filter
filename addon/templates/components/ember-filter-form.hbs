{{!-- <form class="filter-form" {{on "submit" (action "customSearch")}}>
    <form class="filter-form" {{action 'customSearch' on='submit'}}></form>
  <form class="filter-form" onsubmit={{action 'customSearch'}}></form>
  <form class="filter-form" {{on 'submit' this.customSearch}}  --}}
<form class="filter-form" onsubmit={{action 'customSearch'}}></form>
  {{#each-in visibleFilterOptions as |key value|}}
    {{#if value.componentTemplate}}
      {{component value.componentTemplate
         filter=filter
         options=value
         key=key}}
    {{else}}
        {{ember-filter-form-element
          filter=filter
          options=value
          key=key}}
    {{/if}}
  {{/each-in}}
  <div>
    {{#if hasMoreElements}}
      {{component moreOptionTemplate nonVisibleOptions=nonVisibleOptions }}
    {{/if}}
  </div>
  <div class="filter-footer">
    {{component searchButtonTemplate }}
  </div>
</form>
